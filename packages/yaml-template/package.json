{
  "name": "@hdotu1/yaml-template",
  "version": "1.0.0",
  "description": "YAML file parser with the values interpolation",
  "type": "module",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "vitest run",
    "lint": "eslint",
    "lint:fix": "eslint --fix",
    "format": "prettier --check .",
    "format:fix": "prettier --write .",
    "build": "del-cli dist && tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "js-yaml": "^4.1.1",
    "mustache": "^4.2.0"
  },
  "devDependencies": {
    "@types/mustache": "^4.2.6"
  }
}
