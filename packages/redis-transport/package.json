{
  "name": "@hdotu1/redis-transport",
  "version": "1.0.0",
  "description": "NestJS microservices transport strategy backed by @redis/client",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "vitest run",
    "lint": "eslint",
    "lint:fix": "eslint --fix",
    "format": "prettier --check .",
    "format:fix": "prettier --write .",
    "build": "nest build"
  },
  "dependencies": {
    "@redis/client": "^5.10.0"
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.7",
    "@nestjs/testing": "^11.1.2"
  },
  "peerDependencies": {
    "@nestjs/common": "^11.0.0",
    "@nestjs/core": "^11.0.0",
    "@nestjs/microservices": "^11.1.9",
    "rxjs": "^7.8.2"
  }
}
