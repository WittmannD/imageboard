{
  "name": "imageboard-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright end-to-end tests that drive the full imageboard stack",
  "type": "module",
  "scripts": {
    "stack:prepare": "nx run-many -t build --projects=\"@hdotu1/*\"",
    "stack:env": "tsx ../../bin/config-env.ts e2e",
    "stack:up": "npm run stack:env && docker compose -f docker-compose.e2e.yaml --env-file .env.e2e --env-file ../../.generated/config.e2e.env up -d --build --wait",
    "stack:down": "npm run stack:env && docker compose -f docker-compose.e2e.yaml --env-file .env.e2e --env-file ../../.generated/config.e2e.env down -v",
    "stack:logs": "docker compose -f docker-compose.e2e.yaml --env-file .env.e2e --env-file ../../.generated/config.e2e.env logs -f",
    "stack:ps": "docker compose -f docker-compose.e2e.yaml --env-file .env.e2e --env-file ../../.generated/config.e2e.env ps",
    "e2e": "playwright test",
    "e2e:headed": "playwright test --headed",
    "e2e:ui": "playwright test --ui",
    "e2e:report": "playwright show-report",
    "typecheck": "tsc --noEmit",
    "lint": "eslint",
    "lint:fix": "eslint --fix",
    "format": "prettier --check .",
    "format:fix": "prettier --write ."
  },
  "nx": {
    "implicitDependencies": [
      "imageboard-api",
      "imageboard-client",
      "imageboard-identity-provider",
      "image-processor"
    ]
  },
  "dependencies": {
    "@hdotu1/config": "file:../../packages/config"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
